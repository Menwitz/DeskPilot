"""Command-line entry point placeholder for the CLI roadmap phase."""

from __future__ import annotations

from collections.abc import Sequence


def main(argv: Sequence[str] | None = None) -> int:
    """Return a successful placeholder status until CLI commands are added."""

    _ = argv
    return 0
