id: native.settings-bluetooth-open
name: Windows Bluetooth settings open
description: Open Bluetooth settings through the Run dialog for read-only review.
goal: Navigate directly to Bluetooth settings without changing device state.
required_app: Settings
tags:
  - native
  - settings
  - review
inputs:
  - Windows desktop
outputs:
  - Bluetooth settings visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/settings-bluetooth-open.yaml
