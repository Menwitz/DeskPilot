id: native.settings-region-open
name: Windows region settings open
description: Open region settings through the Run dialog for read-only review.
goal: Navigate directly to region settings without changing locale formats.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [region settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-region-open.yaml}
