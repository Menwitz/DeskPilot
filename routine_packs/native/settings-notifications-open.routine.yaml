id: native.settings-notifications-open
name: Windows notifications settings open
description: Open notifications settings through the Run dialog for read-only review.
goal: Navigate directly to notification settings without changing alert behavior.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [notifications settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-notifications-open.yaml}
