id: native.settings-network-open
name: Windows network settings open
description: Open network settings through the Run dialog for read-only review.
goal: Navigate directly to network settings without changing connectivity.
required_app: Settings
tags:
  - native
  - settings
  - review
inputs:
  - Windows desktop
outputs:
  - network settings visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/settings-network-open.yaml
