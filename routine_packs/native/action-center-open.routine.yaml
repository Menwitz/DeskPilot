id: native.action-center-open
name: Action center open
description: Open the Windows notification or quick settings surface for read-only review.
goal: Inspect current Windows notifications or quick settings without changing them.
required_app: Windows
tags:
  - native
  - system
  - notifications
inputs:
  - desktop session
outputs:
  - action center visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/action-center-open.yaml
