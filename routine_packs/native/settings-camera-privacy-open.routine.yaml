id: native.settings-camera-privacy-open
name: Windows camera privacy settings open
description: Open camera privacy settings through the Run dialog for read-only review.
goal: Navigate directly to camera privacy settings without changing app access.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [camera privacy settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-camera-privacy-open.yaml}
