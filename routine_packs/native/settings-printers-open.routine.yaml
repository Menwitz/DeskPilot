id: native.settings-printers-open
name: Windows printers settings open
description: Open printer settings through the Run dialog for read-only review.
goal: Navigate directly to printer settings without adding or changing devices.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [printer settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-printers-open.yaml}
