id: native.settings-personalization-open
name: Windows personalization settings open
description: Open personalization settings through the Run dialog for read-only review.
goal: Navigate directly to personalization settings without changing theme or background.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [personalization settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-personalization-open.yaml}
