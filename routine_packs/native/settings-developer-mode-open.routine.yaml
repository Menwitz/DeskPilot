id: native.settings-developer-mode-open
name: Windows developer mode settings open
description: Open developer mode settings through the Run dialog for read-only review.
goal: Navigate directly to developer settings without enabling developer features.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [developer mode settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-developer-mode-open.yaml}
