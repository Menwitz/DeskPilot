id: native.settings-accessibility-open
name: Windows accessibility settings open
description: Open accessibility settings through the Run dialog for read-only review.
goal: Navigate directly to accessibility settings without changing assistive preferences.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [accessibility settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-accessibility-open.yaml}
