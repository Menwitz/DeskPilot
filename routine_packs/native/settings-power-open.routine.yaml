id: native.settings-power-open
name: Windows power settings open
description: Open power and sleep settings through the Run dialog for read-only review.
goal: Navigate directly to power settings without changing sleep behavior.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [power settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-power-open.yaml}
