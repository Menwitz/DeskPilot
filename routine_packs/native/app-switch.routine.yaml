id: native.app-switch
name: App switch
description: Switch to the next visible application and verify focus changed.
goal: Move between local apps as part of a reviewed workflow.
required_app: Windows desktop
tags:
  - native
  - app-switching
inputs:
  - current app
outputs:
  - focused next app
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 15
reference:
  type: task
  path: tasks/app-switch.yaml
