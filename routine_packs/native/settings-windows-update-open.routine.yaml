id: native.settings-windows-update-open
name: Windows Update settings open
description: Open Windows Update settings through the Run dialog for read-only review.
goal: Navigate directly to Windows Update status without starting update actions.
required_app: Settings
tags:
  - native
  - settings
  - review
inputs:
  - Windows desktop
outputs:
  - Windows Update settings visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/settings-windows-update-open.yaml
