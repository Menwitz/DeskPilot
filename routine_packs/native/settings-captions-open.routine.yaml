id: native.settings-captions-open
name: Windows captions settings open
description: Open captions settings through the Run dialog for read-only review.
goal: Navigate directly to captions settings without changing caption preferences.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [captions settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-captions-open.yaml}
