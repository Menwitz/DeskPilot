name: Show desktop task
allowed_windows:
  - Windows
timeout_seconds: 30
steps:
  - id: show-desktop
    action: press_key
    text: win+d
    verify:
      type: visible_text
      text: Desktop
  - id: confirm-desktop
    action: assert_visible
    verify:
      type: visible_text
      text: Desktop
