id: native.settings-speech-open
name: Windows speech settings open
description: Open speech settings through the Run dialog for read-only review.
goal: Navigate directly to speech settings without changing input preferences.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [speech settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-speech-open.yaml}
