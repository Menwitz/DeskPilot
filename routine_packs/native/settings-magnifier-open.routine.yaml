id: native.settings-magnifier-open
name: Windows magnifier settings open
description: Open Magnifier settings through the Run dialog for read-only review.
goal: Navigate directly to Magnifier settings without changing zoom behavior.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [Magnifier settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-magnifier-open.yaml}
