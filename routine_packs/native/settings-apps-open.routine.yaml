id: native.settings-apps-open
name: Windows apps settings open
description: Open installed apps settings through the Run dialog for read-only review.
goal: Navigate directly to app settings without modifying installed applications.
required_app: Settings
tags:
  - native
  - settings
  - review
inputs:
  - Windows desktop
outputs:
  - apps settings visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/settings-apps-open.yaml
