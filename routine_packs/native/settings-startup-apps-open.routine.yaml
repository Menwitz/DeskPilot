id: native.settings-startup-apps-open
name: Windows startup apps settings open
description: Open startup apps settings through the Run dialog for read-only review.
goal: Navigate directly to startup app settings without enabling or disabling apps.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [startup apps settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-startup-apps-open.yaml}
