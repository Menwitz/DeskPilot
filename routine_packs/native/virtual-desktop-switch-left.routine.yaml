id: native.virtual-desktop-switch-left
name: Virtual desktop switch left
description: Switch to the adjacent virtual desktop on the left.
goal: Move to the previous Windows virtual desktop.
required_app: Windows
tags:
  - native
  - desktop
  - workspace
inputs:
  - desktop session
outputs:
  - previous virtual desktop active
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 15
reference:
  type: task
  path: tasks/virtual-desktop-switch-left.yaml
