id: native.settings-sound-open
name: Windows sound settings open
description: Open sound settings through the Run dialog for read-only review.
goal: Navigate directly to sound settings without changing audio devices.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [sound settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-sound-open.yaml}
