id: native.settings-keyboard-open
name: Windows keyboard settings open
description: Open keyboard settings through the Run dialog for read-only review.
goal: Navigate directly to keyboard settings without changing input behavior.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [keyboard settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-keyboard-open.yaml}
