id: native.settings-narrator-open
name: Windows narrator settings open
description: Open Narrator settings through the Run dialog for read-only review.
goal: Navigate directly to Narrator settings without enabling or changing assistive output.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [Narrator settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-narrator-open.yaml}
