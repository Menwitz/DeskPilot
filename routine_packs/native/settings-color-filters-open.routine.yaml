id: native.settings-color-filters-open
name: Windows color filters settings open
description: Open color filter settings through the Run dialog for read-only review.
goal: Navigate directly to color filter settings without changing accessibility preferences.
required_app: Settings
tags: [native, settings, review]
inputs: [Windows desktop]
outputs: [color filter settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference: {type: task, path: tasks/settings-color-filters-open.yaml}
