id: browser.protected-content-settings-open
name: Browser protected content settings open
description: Open browser protected content settings for read-only review.
goal: Inspect protected content settings without changing media behavior.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [protected content settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/protected-content-settings-open.yaml}
