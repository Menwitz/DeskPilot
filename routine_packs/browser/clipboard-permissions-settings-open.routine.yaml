id: browser.clipboard-permissions-settings-open
name: Browser clipboard permissions settings open
description: Open browser clipboard permission settings for read-only review.
goal: Inspect clipboard permissions without changing site access.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [clipboard permissions visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/clipboard-permissions-settings-open.yaml}
