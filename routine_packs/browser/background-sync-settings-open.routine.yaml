id: browser.background-sync-settings-open
name: Browser background sync settings open
description: Open browser background sync settings for read-only review.
goal: Inspect background sync permissions without changing site behavior.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [background sync settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/background-sync-settings-open.yaml}
