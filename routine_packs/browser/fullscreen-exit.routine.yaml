id: browser.fullscreen-exit
name: Browser exit full screen
description: Exit browser full-screen mode and verify the browser surface remains active.
goal: Return a full-screen browser window to normal view.
required_app: Browser
tags:
  - browser
  - viewing
  - fullscreen
inputs:
  - full screen browser
outputs:
  - normal browser view restored
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference:
  type: task
  path: tasks/fullscreen-exit.yaml
