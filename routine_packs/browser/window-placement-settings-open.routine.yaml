id: browser.window-placement-settings-open
name: Browser window placement settings open
description: Open browser window placement permission settings for read-only review.
goal: Inspect window placement permissions without changing site access.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [window placement settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/window-placement-settings-open.yaml}
