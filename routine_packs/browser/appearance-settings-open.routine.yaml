id: browser.appearance-settings-open
name: Browser appearance settings open
description: Open browser appearance settings for read-only review.
goal: Inspect theme and toolbar settings without changing the browser UI.
required_app: Browser
tags: [browser, settings, appearance]
inputs: [browser settings]
outputs: [appearance settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/appearance-settings-open.yaml}
