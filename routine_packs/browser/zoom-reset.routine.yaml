id: browser.zoom-reset
name: Browser zoom reset
description: Exercise zoom in, zoom out, and reset browser zoom to the default level.
goal: Return the current page to a readable default zoom level.
required_app: Browser
tags:
  - browser
  - viewing
  - accessibility
inputs:
  - current page
outputs:
  - default zoom restored
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/zoom-reset.yaml
