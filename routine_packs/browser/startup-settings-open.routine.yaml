id: browser.startup-settings-open
name: Browser startup settings open
description: Open browser startup settings for read-only review.
goal: Inspect startup behavior without changing launch configuration.
required_app: Browser
tags: [browser, settings, startup]
inputs: [browser settings]
outputs: [startup settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/startup-settings-open.yaml}
