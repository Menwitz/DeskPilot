id: browser.idle-detection-settings-open
name: Browser idle detection settings open
description: Open browser idle detection permission settings for read-only review.
goal: Inspect idle detection permissions without changing site access.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [idle detection settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/idle-detection-settings-open.yaml}
