id: browser.tab-search
name: Browser tab search
description: Open browser tab search and enter a reviewed tab title fragment.
goal: Locate an open tab by title from the browser tab search surface.
required_app: Browser
tags:
  - browser
  - tabs
  - search
inputs:
  - tab title fragment
outputs:
  - tab search results visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference:
  type: task
  path: tasks/tab-search.yaml
