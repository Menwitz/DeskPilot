id: browser.cleanup-settings-open
name: Browser cleanup settings open
description: Open browser cleanup or reset assistance settings for read-only review.
goal: Inspect cleanup settings without resetting or removing data.
required_app: Browser
tags: [browser, settings, review]
inputs: [browser settings]
outputs: [cleanup settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/cleanup-settings-open.yaml}
