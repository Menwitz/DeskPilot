id: browser.downloads-settings-open
name: Browser downloads settings open
description: Open browser downloads settings for read-only review.
goal: Inspect download behavior settings without changing paths or prompts.
required_app: Browser
tags: [browser, settings, downloads]
inputs: [browser settings]
outputs: [downloads settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/downloads-settings-open.yaml}
