id: browser.local-fonts-settings-open
name: Browser local fonts settings open
description: Open browser local font permission settings for read-only review.
goal: Inspect local font permissions without changing site access.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [local fonts settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/local-fonts-settings-open.yaml}
