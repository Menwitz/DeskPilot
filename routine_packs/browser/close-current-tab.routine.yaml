id: browser.close-current-tab
name: Browser close current tab
description: Close the active browser tab after confirming a browser window is active.
goal: Remove the current tab from the browser workspace.
required_app: Browser
tags:
  - browser
  - navigation
  - tabs
inputs:
  - active tab
outputs:
  - tab closed
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 15
reference:
  type: task
  path: tasks/close-current-tab.yaml
