id: browser.reload-page
name: Browser reload page
description: Reload the current browser page and verify the page surface returns.
goal: Refresh the current page in a controlled browser workflow.
required_app: Browser
tags:
  - browser
  - navigation
  - refresh
inputs:
  - current page
outputs:
  - refreshed page visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 20
reference:
  type: task
  path: tasks/reload-page.yaml
