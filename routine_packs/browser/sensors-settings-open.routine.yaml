id: browser.sensors-settings-open
name: Browser sensors settings open
description: Open browser sensor permission settings for read-only review.
goal: Inspect sensor permissions without changing device access.
required_app: Browser
tags: [browser, settings, permissions]
inputs: [browser settings]
outputs: [sensor settings visible]
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference: {type: task, path: tasks/sensors-settings-open.yaml}
