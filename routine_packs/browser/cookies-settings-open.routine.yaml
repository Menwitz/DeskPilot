id: browser.cookies-settings-open
name: Browser cookies settings open
description: Open browser cookies and site data settings for read-only review.
goal: Inspect cookies settings without changing stored data.
required_app: Browser
tags:
  - browser
  - settings
  - cookies
inputs:
  - browser profile
outputs:
  - cookies settings visible
safety_class: low
schedule_policy: manual
approval_policy: none
expected_duration_seconds: 25
reference:
  type: task
  path: tasks/cookies-settings-open.yaml
